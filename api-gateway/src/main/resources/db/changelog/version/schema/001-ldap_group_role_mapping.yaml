databaseChangeLog:
  - changeSet:
      id: schema-v001
      author: valex
      changes:
        - sql:
            sql: |
              create table sys.ldap_group_role_mapping
                (
                id            bigint generated by default as identity primary key,
                ldap_group_dn varchar(255),
                role          varchar(255)
                );

