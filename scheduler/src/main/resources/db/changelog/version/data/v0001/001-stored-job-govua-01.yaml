databaseChangeLog:
  - changeSet:
      id: data-v0001-001
      author: valex
      changes:
        - sql:
            sql: |
              insert into scheduler.stored_job(id, source, name, short_name, is_enabled)
              values (1, 'Державна судова адміністрація України', 'Відомості про справи про банкрутство',
              'govua01', true);
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (1, 1, 1, 'govua01', 'downloader', true, false);
              
              
              insert into scheduler.stored_step_parameter (stored_step_id, key, value)
              values (1, 'retries', '10'),
              (1, 'packageId', 'vidomosti-pro-spravi-pro-bankrutstvo-1'),
              (1, 'timeoutSec', '30');
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (2, 1, 2, 'govua01', 'importer', true, false);
              
                
              
              
