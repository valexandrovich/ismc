databaseChangeLog:
  - changeSet:
      id: data-v0001-004
      author: valex
      changes:
        - sql:
            sql: |
              insert into scheduler.stored_job(id, source, name, short_name, is_enabled)
              values (9, 'Міністерство внутрішніх справ України', 'Інформація про безвісно зниклих громадян',
              'govua09', true);
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (7, 9, 1, 'govua09', 'downloader', true, false);
              
              
              insert into scheduler.stored_step_parameter (stored_step_id, key, value)
              values (7, 'retries', '10'),
              (7, 'packageId', '470196d3-4e7a-46b0-8c0c-883b74ac65f0'),
              (7, 'timeoutSec', '30');
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (8, 9, 2, 'govua09', 'importer', true, false);