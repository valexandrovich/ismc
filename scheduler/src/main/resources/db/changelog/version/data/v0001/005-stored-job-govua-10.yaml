databaseChangeLog:
  - changeSet:
      id: data-v0001-005
      author: valex
      changes:
        - sql:
            sql: |
              insert into scheduler.stored_job(id, source, name, short_name, is_enabled)
              values (10, 'Державна міграційна служба України', 'Паспорти громадянина України, які визнані недійсними',
              'govua10', true);
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (9, 10, 1, 'govua10', 'downloader', true, false);
              
              
              insert into scheduler.stored_step_parameter (stored_step_id, key, value)
              values (9, 'retries', '10'),
              (9, 'packageId', '44e1d462-5de4-40e5-b722-46f2aa9a1e81'),
              (9, 'timeoutSec', '30');
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (10, 10, 2, 'govua10', 'importer', true, false);
