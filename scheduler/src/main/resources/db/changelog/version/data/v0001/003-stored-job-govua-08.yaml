databaseChangeLog:
  - changeSet:
      id: data-v0001-003
      author: valex
      changes:
        - sql:
            sql: |
              insert into scheduler.stored_job(id, source, name, short_name, is_enabled)
              values (8, 'Міністерство внутрішніх справ України', 'Інформація про осіб, які переховуються від органів влади',
              'govua08', true);
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (5, 8, 1, 'govua08', 'downloader', true, false);
              
              
              insert into scheduler.stored_step_parameter (stored_step_id, key, value)
              values (5, 'retries', '10'),
              (5, 'packageId', '7c51c4a0-104b-4540-a166-e9fc58485c1b'),
              (5, 'timeoutSec', '30');
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (6, 8, 2, 'govua08', 'importer', true, false);