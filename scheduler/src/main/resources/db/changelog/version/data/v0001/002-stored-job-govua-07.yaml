databaseChangeLog:
  - changeSet:
      id: data-v0001-002
      author: valex
      changes:
        - sql:
            sql: |
              insert into scheduler.stored_job(id, source, name, short_name, is_enabled)
              values (7, 'Міністерство юстиції України', 'Єдиний реєстр боржників',
              'govua07', true);
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (3, 7, 1, 'govua07', 'downloader', true, false);
              
              
              insert into scheduler.stored_step_parameter (stored_step_id, key, value)
              values (3, 'retries', '10'),
              (3, 'packageId', '506734bf-2480-448c-a2b4-90b6d06df11e'),
              (3, 'timeoutSec', '30');
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (4, 7, 2, 'govua07', 'importer', true, false);
              
                
              
              
