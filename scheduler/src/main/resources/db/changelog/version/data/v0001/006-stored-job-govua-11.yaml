databaseChangeLog:
  - changeSet:
      id: data-v0001-006
      author: valex
      changes:
        - sql:
            sql: |
              insert into scheduler.stored_job(id, source, name, short_name, is_enabled)
              values (11, 'Державна міграційна служба України', 'Паспорти громадянина України для виїзду за кордон, які визнані недійсними',
              'govua11', true);
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (11, 11, 1, 'govua11', 'downloader', true, false);
              
              
              insert into scheduler.stored_step_parameter (stored_step_id, key, value)
              values (11, 'retries', '10'),
              (11, 'packageId', '672e0841-e1a2-47ec-b8d4-22839c71f4b3'),
              (11, 'timeoutSec', '30');
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (12, 11, 2, 'govua11', 'importer', true, false);
