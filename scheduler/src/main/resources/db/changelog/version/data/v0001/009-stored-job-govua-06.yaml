databaseChangeLog:
  - changeSet:
      id: data-v0001-009
      author: valex
      changes:
        - sql:
            sql: |
              insert into scheduler.stored_job(id, source, name, short_name, is_enabled)
              values (6, 'Міністерство юстиції України', 'Інформація з автоматизованої системи виконавчого провадження',
              'govua06', true);
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (17, 6, 1, 'govua06', 'downloader', true, false);
              
              
              insert into scheduler.stored_step_parameter (stored_step_id, key, value)
              values (17, 'retries', '10'),
              (17, 'packageId', '6c0eb6c0-d19a-4bb0-869b-3280df46800a'),
              (17, 'timeoutSec', '30');
              
              insert into scheduler.stored_step(id, stored_job_id, step_order, worker, service, is_enabled, is_skippable)
              values (18, 6, 2, 'govua06', 'importer', true, false);
