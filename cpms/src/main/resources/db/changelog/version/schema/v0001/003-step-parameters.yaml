databaseChangeLog:
  - changeSet:
      id: schema-v0001-003
      author: valex
      changes:
        - sql:
            sql: |
              create table cpms.step_parameters
                (
                step_id bigint  not null constraint fk__step_parameters__step_id references cpms.step,
                key     varchar(255) not null,
                value   varchar(255),
                primary key (step_id, key)
                );
