databaseChangeLog:
  - changeSet:
      id: schema-v0001-004
      author: valex
      changes:
        - sql:
            sql: |
              create table cpms.job_results
                (
                job_id bigint not null constraint fk__job_results__job_id references cpms.job,
                key    varchar(255) not null,
                value  varchar(255),
                primary key (job_id, key)
                );

